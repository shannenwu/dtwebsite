import React, { Component } from 'react';
import PropTypes from 'prop-types';
import { List, Popup } from 'semantic-ui-react';
class TimeSelection extends Component {
  constructor(props) {
    super(props);
  }

  static propTypes = {
  }

  static defaultProps = {
  }

  componentDidMount() {
  }

  render() {
    const {
    } = this.props;

    return (<div>CHOOSE A TIME!</div>)
  }
}

export default TimeSelection;
